# 失效站点

由 `scripts/build-site-meta.mjs` 从 `app.js` 的 `siteConfig.archivedEntries` 生成，请勿手改。

这里存放已经从页面下架的站点，只作留档。**下面的链接都不再推荐访问。**
站点恢复时，把 `archivedEntries` 里对应的对象移回 `siteConfig.entries`，并把名字加回 `displayOrder`，
然后重新跑一次 `node scripts/build-site-meta.mjs`。英文文案一直留在 `entryTranslations` 里，不需要搬。

共 1 个。

## Zynk 公益站

注册后联系群主可领取 200 额度；支持每日签到，签到金额随机，8 月 14 日和 8 月 15 日实测均为 15。

| 项目 | 记录 |
| --- | --- |
| 下架日期 | 2026-08-29 |
| 下架原因 | 站点已失效，域名无法正常使用；若恢复再移回 entries。 |
| 原分类 | 公益新站 |
| 首次收录 | 2026-08-15 10:38 |
| 注册方式 | 通过邀请链接注册，进入后联系群主领取 200 额度。 |
| 注册赠送 | 联系群主送 200 |
| 每日签到 | 随机；最近两次均为 15 |
| 速度与稳定性 | 新站暂不稳定，建议先小量使用 |
| 当时的注意事项 | 每个月必须使用超过 100 额度，否则可能删号；签到金额为随机值。 |
| 原链接（不再推荐访问） | `https://gy.leyanshi.me/sign-up?aff=lorI` |

当时记录的福利：联系群主送 200、每日随机签到、8 月 14 日签到 15、8 月 15 日签到 15
